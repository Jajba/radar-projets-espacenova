# Local: laisser vide pour SQLite.
# Production Scaleway PostgreSQL :
# DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require
REQUEST_TIMEOUT=25
MAX_LINKS_PER_SOURCE=80
MIN_TEXT_CHARS=250
